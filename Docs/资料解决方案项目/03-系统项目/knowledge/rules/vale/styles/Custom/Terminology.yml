extends: substitution
message: "使用 '%s' 替代 '%s'"
level: error
ignorecase: true
swap:
  api: API
  sdk: SDK
  ui: UI
  json: JSON
  yaml: YAML
  cli: CLI
  rest: REST
  http: HTTP
  ssh: SSH
  mcp: MCP
