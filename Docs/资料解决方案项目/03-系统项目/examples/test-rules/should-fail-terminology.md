# Test Document - Should Fail

This document uses incorrect terminology that should trigger the rule.

The api provides an sdk for developers to build ui components.
The json configuration uses yaml format.
The cli tool supports rest api calls over http.

These terms should be capitalized according to the terminology rule.
